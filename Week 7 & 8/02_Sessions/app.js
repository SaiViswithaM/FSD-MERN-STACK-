const express=require("express"); const session=require("express-session"); const app=express(); app.set("view engine","ejs"); app.use(express.urlencoded({extended:true})); app.use(session({secret:"fsd-secret",resave:false,saveUninitialized:false,cookie:{maxAge:1800000,httpOnly:true}})); app.get("/",(req,res)=>req.session.username?res.render("dashboard",{username:req.session.username}):res.render("login")); app.post("/login",(req,res)=>{if(req.body.username==="sai"&&req.body.password==="1234"){req.session.username="sai";return res.redirect("/")}res.send("Invalid credentials. Try sai / 1234")}); app.get("/logout",(req,res)=>req.session.destroy(()=>res.redirect("/"))); app.listen(3002,()=>console.log("http://localhost:3002"));